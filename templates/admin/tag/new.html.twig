{% extends '@EasyAdmin/default/new.html.twig' %}

{% block main %}
    {{ form_start(form) }}
        {{ form_row(form.title) }}&nbsp;

        <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> {{ 'Сохранить'|trans }}
        </button>
    {{ form_end(form) }}
{% endblock %}